global:
  scrape_interval: 5s

scrape_configs:
  - job_name: language-learning
    metrics_path: /metrics
    static_configs:
      - targets: ["host.docker.internal:8080"]
        labels:
          service: language-learning
